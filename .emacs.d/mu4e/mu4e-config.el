;; auto-generated

(defconst mu4e-mu-version "1.8.9"
  "Required mu binary version; mu4e's version must agree with this.")

(defconst mu4e-builddir "/var/tmp/pamac-build-tutict/mu/src/mu-1.8.9/build/mu4e"
  "Top-level build directory.")

(defconst mu4e-doc-dir "/usr/share/doc/mu"
  "Mu4e's data-dir.")

(provide 'mu4e-config)
