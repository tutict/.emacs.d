import { CancellationToken } from 'vscode-languageserver';
import * as lsp from 'vscode-languageserver-types';
import { LsConfiguration } from '../config';
import { MdTableOfContentsProvider } from '../tableOfContents';
import { ITextDocument } from '../types/textDocument';
import { IWorkspace } from '../workspace';
import { MdWorkspaceInfoCache } from '../workspaceCache';
import { MdLink } from './documentLinks';
export declare class MdDefinitionProvider {
    private readonly configuration;
    private readonly workspace;
    private readonly tocProvider;
    private readonly linkCache;
    constructor(configuration: LsConfiguration, workspace: IWorkspace, tocProvider: MdTableOfContentsProvider, linkCache: MdWorkspaceInfoCache<readonly MdLink[]>);
    provideDefinition(document: ITextDocument, position: lsp.Position, token: CancellationToken): Promise<lsp.Definition | undefined>;
    private getDefinitionOfLinkAtPosition;
    private getDefinitionOfLink;
    private getDefinitionOfRef;
}
