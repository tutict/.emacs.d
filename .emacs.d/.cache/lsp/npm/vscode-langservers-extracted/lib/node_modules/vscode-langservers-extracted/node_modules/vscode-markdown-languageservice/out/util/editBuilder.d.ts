import * as lsp from 'vscode-languageserver-types';
import { URI } from 'vscode-uri';
export declare class WorkspaceEditBuilder {
    private readonly changes;
    private readonly documentChanges;
    replace(resource: URI, range: lsp.Range, newText: string): void;
    getEdit(): lsp.WorkspaceEdit;
    renameFile(targetUri: URI, resolvedNewFilePath: URI): void;
}
