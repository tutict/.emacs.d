import * as lsp from 'vscode-languageserver-types';
import { ILogger } from '../logging';
import { MdTableOfContentsProvider } from '../tableOfContents';
import { ITextDocument } from '../types/textDocument';
export declare class MdDocumentSymbolProvider {
    private readonly tocProvider;
    private readonly logger;
    constructor(tocProvider: MdTableOfContentsProvider, logger: ILogger);
    provideDocumentSymbolInformation(document: ITextDocument): Promise<lsp.SymbolInformation[]>;
    provideDocumentSymbols(document: ITextDocument): Promise<lsp.DocumentSymbol[]>;
    private buildTree;
    private toSymbolInformation;
    private toDocumentSymbol;
    private getSymbolName;
}
